group 'ca.six'
version '1.0'

apply plugin: 'groovy'

repositories {
    mavenCentral()
}

dependencies {
    compile 'org.codehaus.groovy:groovy-all:2.4.11'
    compile gradleApi() // 不加这个就找不到DefaultTask了

    testCompile group: 'junit', name: 'junit', version: '4.12'
}
